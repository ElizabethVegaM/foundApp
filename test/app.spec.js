describe('fadeout', () => {

  it('should be a function', () => {
    assert.equal(typeof fadeout, 'function');
  });

});

describe('disappear', () => {

  it('should be a function', () => {
    assert.equal(typeof disappear, 'function');
  });

});
