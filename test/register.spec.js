describe('showPersonForm', () => {

  it('should be a function', () => {
    assert.equal(typeof showPersonForm, 'function');
  });

});

describe('showObjectForm', () => {

  it('should be a function', () => {
    assert.equal(typeof showObjectForm, 'function');
  });

});
